[build]
  publish = "."
  functions = "netlify/functions"

[functions]
  node_bundler = "nft"

# Redirect API calls to functions
[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/:splat"
  status = 200
